import TrackBrowser from "@/components/hub/interview/TrackBrowser";

export const metadata = { title: "Interview Prep — SE Hub" };

export default function InterviewPage() {
  return <TrackBrowser />;
}
